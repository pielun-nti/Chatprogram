package models;

/**
 * Info class with just variables.
 */
public class Info {
    /**
     * Clientsconnected variable is how many clients that are connected.
     */
    public static int clientsConnected = 0;
    /**
     * Max clients connected is an integer of max limit for client connections.
     */
    public static int maxClientsConnected = 50;
}
